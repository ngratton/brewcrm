import tpl from '../utils/avecTemplateHtml.js'
import { http_get } from '../utils/request.js'

export default Vue.component('navmenu', tpl({
    template: 'components/navmenu.html',
    data() {
        return {

        }
    },
}))
